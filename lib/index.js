export const toto = 123;

/**
 * Defines the shape of a JSON value.
 * @typedef {null|boolean|number|string|Json[]|Record<string, JsonObject>} Json
 */

/**
 * Defines the shape of an object in JSON format.
 * @typedef {Record<string, Json>} JsonObject
 */
